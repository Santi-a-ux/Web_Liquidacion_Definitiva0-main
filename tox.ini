[tox]
envlist = py39
skipsdist = True

[testenv]
deps =
    pytest
    coverage
    psycopg2
setenv =
    PYTHONPATH = {toxinidir}/src
commands =
    coverage run -m pytest
    coverage xml

[coverage:run]
relative_files = True
source = src
branch = True
